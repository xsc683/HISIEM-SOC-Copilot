"""Shared test-side support code.

Deliberately NOT named ``test_*.py``: pytest collects by filename, and a helper
module that pytest tries to collect as a test module is a bug waiting to happen.
"""
