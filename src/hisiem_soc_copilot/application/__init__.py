"""Application layer — use cases, commands, queries, ports.

Application depends on domain + stdlib only; external capabilities come in as
Protocols/ABCs. Application never imports ``infrastructure``.
"""
